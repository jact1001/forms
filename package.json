{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "@types/enzyme": "^3.10.10",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test --scope=items-web"
  },
  "workspaces": [
    "packages/*"
  ]
}
